services:
  - type: web
    name: storage-owner-finder-mvp
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn storage_owner_finder.web:app --host 0.0.0.0 --port $PORT
